name: CI Health Probe

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  probe:
    name: Actions runner smoke
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Runner OK
        run: echo "ci-health-probe ok"
