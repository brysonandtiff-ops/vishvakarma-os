# Certification-only branch note

The direct certification runner and phase deployments are temporary release infrastructure. They must not be merged into production `main` or included in the frozen `v1.5.0` tag.
