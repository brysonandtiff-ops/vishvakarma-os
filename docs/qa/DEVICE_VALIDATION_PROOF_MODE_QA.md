# Device Validation Proof Mode QA

Device Validation Proof Mode provides a local-only QA panel for release checks across iPad, mobile, desktop, auth, editor, guided demo, grid, and safe touch targets.

Use the floating QA button to open the panel.

The panel stores checklist state in localStorage only. It does not change Supabase, auth, routes, projects, or user data.