# Solar Mandala PR branch note

The `ui/solar-mandala-identity` branch has been rebuilt from current green `main` and carries the visual-theme branch work separately from `main`.

- Main was not modified by this note.
- The visual theme controller is kept below modal overlays with `z-index: 40`.
- The branch includes a regression test guard for the layering contract.
