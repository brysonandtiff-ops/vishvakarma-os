# Vishvakarma.OS UI Page Image Set

Generated from the connected GitHub repository route and page source.

## Pages generated

1. `/auth` — Account Access / secure access gate
2. `/` — Blueprint Editor workspace
3. `/spec-center` — Spec Center
4. `/registry` — Registry Center
5. `/change-requests` — Change Requests
6. `/releases` — Release Center
7. `/audit` — Audit Log

## Notes

These are repo-sourced visual recreations based on the raw React/Tailwind page structure, route manifest, page headings, empty states, governance cards, dialogs, stats rows, and layout patterns visible in source. They are not live browser screenshots because the private GitHub connector exposes file contents, not a runnable authenticated checkout inside this environment.
