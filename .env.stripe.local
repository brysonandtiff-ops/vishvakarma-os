# SECURITY INCIDENT — credentials removed from the repository.
# Never store secrets in Git. Use Vercel encrypted environment variables and local ignored .env files.
# The previously committed Google service-account key and Stripe credentials must be treated as compromised and rotated.
