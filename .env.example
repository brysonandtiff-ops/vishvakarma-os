# Vishvakarma.OS environment template
# Copy this file to .env.local for local development or configure these variables in Vercel.

# E2E only (Playwright app-smoke project): set true in e2e build to access editor without auth when backend is unconfigured.
# VITE_E2E_ALLOW_LOCAL_ACCESS=true

# ============================================================================
# Firebase Authentication + Firestore (required for cloud save and governance)
# ============================================================================
# Without these values, the app runs in Local Draft mode.
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-firebase-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
VITE_FIREBASE_APP_ID=your-firebase-app-id

# Firebase setup checklist:
# 1. In Firebase console > Authentication > Sign-in method, enable Email link (passwordless sign-in).
# 2. Enable Google and Apple providers if needed.
# 3. Add production and localhost preview URLs to authorized domains.
# 4. Create a Firestore database and deploy firestore.rules from this repo.
# 5. See docs/release/FIREBASE_CUTOVER.md for migration from Supabase.

# Migration scripts (operator only — not used by the SPA):
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
# GOOGLE_APPLICATION_CREDENTIALS=path/to/firebase-service-account.json

# ============================================================================
# Local development
# ============================================================================
# Set to "true" in DEV to bypass the auth gate for local preview work.
# VITE_ALLOW_LOCAL_DEMO=true
